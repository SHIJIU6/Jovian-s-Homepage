{
  "name": "javian-homepage",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node ./scripts/build-assets.mjs",
    "dev": "npm run build && wrangler pages dev public --kv SITE_CONFIG --kv AUTH_TOKENS --r2 IMAGES",
    "deploy": "npm run build && wrangler pages deploy public",
    "kv:create": "wrangler kv:namespace create SITE_CONFIG && wrangler kv:namespace create AUTH_TOKENS",
    "r2:create": "wrangler r2 bucket create javian-images"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.10",
    "@tailwindcss/typography": "^0.5.16",
    "esbuild": "^0.17.19",
    "tailwindcss": "^3.4.17",
    "wrangler": "^3.0.0"
  }
}
